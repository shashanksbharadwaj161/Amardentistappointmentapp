import { createClient } from 'npm:@supabase/supabase-js@2.112.4';import { json } from '../_shared/http.ts';import { isProvider,isUuid,type Provider } from '../_shared/payment.ts'
export type RefundGateway=(input:{provider:Provider,providerPaymentId:string,amountBdt:number,idempotencyKey:string})=>Promise<{providerRefundId:string,raw:unknown}>
export type RefundConfig={supabaseUrl?:string;publishableKey?:string;serviceRoleKey?:string;bkashRefundUrl?:string;bkashToken?:string;nagadRefundUrl?:string;nagadToken?:string}
function liveGateway(config:RefundConfig):RefundGateway{return async({provider,providerPaymentId,amountBdt,idempotencyKey})=>{const endpoint=provider==='bkash'?config.bkashRefundUrl:config.nagadRefundUrl;const token=provider==='bkash'?config.bkashToken:config.nagadToken;if(!endpoint||!token)throw new Error('PROVIDER_NOT_CONFIGURED');const response=await fetch(endpoint,{method:'POST',headers:{authorization:`Bearer ${token}`,'content-type':'application/json','idempotency-key':idempotencyKey},body:JSON.stringify({paymentId:providerPaymentId,amount:amountBdt.toFixed(2)})});const raw=await response.json().catch(()=>({})) as Record<string,unknown>;if(!response.ok)throw new Error('PROVIDER_REFUND_FAILED');const providerRefundId=String(raw.refundTrxID??raw.refundId??'');if(!providerRefundId)throw new Error('PROVIDER_RESPONSE_INVALID');return{providerRefundId,raw}}}
export function createRefundHandler(config:RefundConfig,clientFactory:typeof createClient=createClient,gateway=liveGateway(config)){return async(request:Request)=>{if(!config.supabaseUrl||!config.publishableKey||!config.serviceRoleKey)return json(503,{ok:false,error:{code:'SERVICE_NOT_CONFIGURED'}});if(request.method!=='POST')return json(405,{ok:false,error:{code:'METHOD_NOT_ALLOWED'}});const authorization=request.headers.get('authorization');if(!authorization)return json(401,{ok:false,error:{code:'UNAUTHENTICATED'}});let body:{paymentId?:string;amountBdt?:number;reason?:string;idempotencyKey?:string;provider?:unknown;providerPaymentId?:string};try{body=await request.json()}catch{return json(400,{ok:false,error:{code:'INVALID_INPUT'}})}if(!isUuid(body.paymentId)||!isProvider(body.provider)||!body.providerPaymentId||typeof body.amountBdt!=='number'||body.amountBdt<=0||!body.reason||body.reason.trim().length<3||!body.idempotencyKey||body.idempotencyKey.length<12)return json(400,{ok:false,error:{code:'INVALID_INPUT'}});const caller=clientFactory(config.supabaseUrl,config.publishableKey,{global:{headers:{authorization}}});const auth=await caller.auth.getUser();if(auth.error||!auth.data.user)return json(401,{ok:false,error:{code:'UNAUTHENTICATED'}});const prepared=await caller.rpc('prepare_refund',{target_payment_id:body.paymentId,refund_amount:body.amountBdt,refund_reason:body.reason,request_key:body.idempotencyKey});if(prepared.error||!prepared.data)return json(403,{ok:false,error:{code:'REFUND_PREPARATION_FAILED'}});try{const result=await gateway({provider:body.provider,providerPaymentId:body.providerPaymentId,amountBdt:body.amountBdt,idempotencyKey:body.idempotencyKey});const service=clientFactory(config.supabaseUrl,config.serviceRoleKey,{auth:{persistSession:false}});const applied=await service.rpc('apply_refund_result',{target_refund_id:prepared.data,target_status:'succeeded',provider_reference:result.providerRefundId,event_payload:result.raw});if(applied.error)return json(500,{ok:false,error:{code:'REFUND_SAVE_FAILED'}});return json(200,{ok:true,data:{refundId:prepared.data,status:'succeeded'}})}catch(error){return json(502,{ok:false,error:{code:error instanceof Error?error.message:'PROVIDER_REFUND_FAILED'}})}}}

